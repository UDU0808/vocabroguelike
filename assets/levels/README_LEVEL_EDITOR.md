# 迷宫大地图关卡编辑说明

文件：`nes_bomberman_layout.json`

字符说明：
- `#` = 固定硬墙，不可破坏，不可穿越，不可闪现越过
- `B` = 可破坏砖墙，可以被翻译弹打破
- `.` = 空地
- `S` = 出生安全区

注意：
1. 每一行长度建议保持 29 个字符。
2. 行数建议保持 17 行。
3. 硬墙和砖墙现在都是完整 64×64 模块，连续写多个 `#` 或 `B` 就会连续拼接。
